<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techtraining.cosechasapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ItemSueltosActivity" android:parentActivityName=".LlenadoSueltosActivity"></activity>
        <activity
            android:name=".LlenadoSueltosActivity"
            android:parentActivityName=".LlenadoCamion" />
        <activity
            android:name=".ItemCosechaActivity"
            android:parentActivityName=".LlenadoCamion" />
        <activity
            android:name=".FilaCosechaCamionActivity"
            android:parentActivityName=".LlenadoCamion" />
        <activity
            android:name=".LlenadoCamion"
            android:parentActivityName=".NuevaCosechaActivity" />
        <activity
            android:name=".CosechasTerminadasActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".NuevaCosechaActivity"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>